# Local development only. Change these values when the database is exposed.
POSTGRES_IMAGE=postgres:17-alpine
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=quant_foundry
POSTGRES_PORT=5432
