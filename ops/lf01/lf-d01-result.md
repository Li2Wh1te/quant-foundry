# LF-D01 v1.1 结果

状态：**ready_for_review**。本包代码已经推送至独立 PR [#115](https://github.com/Li2Wh1te/quant-foundry/pull/115)，仓库回归和真实跨容器/受支持文件系统验收均已实际通过。合并前仍须确认最终 PR head 的两套 CI 成功；不以这份说明代替 GitHub 的实际合并记录。

本文件更新的是 LF-D01 独立开发包，不代表 LF-01 整体 code_ready。前一轮 local-only partial 报告原文保留在 [lf-d01-local-result.md](lf-d01-local-result.md)，不再用其中的“未推送/未补验”作为当前状态。

## 实际提交、PR 与前序输入

仓库 `Li2Wh1te/quant-foundry`，前序真实输入 `e09f41d093c665ea6ca34af05168864f79445f9f`，来自广义 LF-01 草稿 PR #114。D01 的五个补丁在该真实祖先上应用，未推送本地快照根或强制改写分支。新分支为 `codex/lf-d01-current-store`；#114 没有被当作整个 LF-01 完成而合并。

已补验的功能提交为 `4a2d00a69d1b940f9b81fa2f9ecfdc1885d8d2a7`，树 `998ff5da39af4d1a77f142b7052770370b3f27ef`，逐项与本地交付一致。本次随后仅更新结果/进度文档；最终 head 的实际检查在 PR Conversation 留证。临时传输 workflow 不在本 PR 中，原临时离线导出 workflow 已移除；隔离验收 workflow 权限仅 `contents: read`。

实际环境：**开发隔离**。原本地实现约 2026-09-25 02:36–03:31 UTC；本次 GitHub 回归及隔离补验约 04:11–04:20 UTC。没有生产执行。

## 已完成与内部接口

D01 当前目录、显式类型压缩 Parquet、进程内 DuckDB、原子目录/问题/checkpoint/generation 提交、提交结果不明的确认、分页变化检测、多依赖读锁、共享资源预算、受限清理和故障恢复已实现。没有历史发布父链、逐行成功流程账或第二份持久化业务全库。

后续 D02/D03/D04 的调用签名、前置条件、半开区间/完整报告语义、来源确认谓词、问题 token、限制及错误码见 [lf-d01-interfaces.md](lf-d01-interfaces.md)。新增迁移 `20261005_01` 仅增加当前存储结构，不删除旧底座、不初始化生产存储、不注册新公共 API 或默认调度。

## 检查结果：通过

已直接读取以下运行状态、日志和产物，不借用旧提交的测试结论。

| 检查 | 结果 | 实际运行 |
| --- | --- | --- |
| 完整仓库 Validate | success | [36093438008](https://github.com/Li2Wh1te/quant-foundry/actions/runs/36093438008)，job `107940502088` |
| 后端，Python 3.12.2 / PostgreSQL 17 启用 | 3301 passed、1 skipped、234 subtests passed；264.32 秒 | 同一 Validate 日志 |
| 根目录运维/版本 | 19 passed；版本 0.3.0 一致 | 同一 Validate 日志 |
| 前端 | 92 passed；TypeScript 和生产构建通过 | 同一 Validate 日志 |
| 完整迁移、后端 Docker / Parquet 运行检查 | 迁移至 `20261005_01`，全部成功 | 同一 Validate 日志 |
| Current store isolated acceptance | success；8 个跨容器步骤全部 passed | [36093438087](https://github.com/Li2Wh1te/quant-foundry/actions/runs/36093438087)，job `107940502325` |
| 支持文件系统原生内核/schema测试 | 59 passed，1 warning；53.36 秒 | 同一隔离验收产物 |
| B01–B04 | 11 项全部 passed；complete=true | 同一隔离验收产物 |

跨容器实测包含双写者竞争、SIGKILL 后锁释放、活动读者期间提交阻塞、并行 cleanup、目录切换后读取和旧文件回收。文件系统实测为 **ext4**，`filesystem_probe_injected=false`，不是上一轮的 overlay 探测替换。容器采用同一真实 bind mount，存储容器 CPU 配额为 1、内存限制 2 GiB；隔离 PostgreSQL 为单独容器。主机 CPU affinity 显示 4 个逻辑 CPU，不应误写为存储容器拥有 4 核配额。

原 C01/C09/C11/C12/C13/C15/C16 的机制测试和真实原生检查通过。本轮没有真实断电实验，也没有分别认证 XFS/Btrfs；不从 ext4 的成功外推全部文件系统或生产持久性。C09 使用隔离 HTTP 序列化往返，不代表 D04 公共 API 已注册。原有 skip、弃用和前端体积警告未删除；59 项原生子集与完整后端有重叠，不叠加为“新增测试总数”。

## B01–B04 实测摘要

| 原场景 | 规模/次数 | 秒 | 当前文件数 | 逻辑当前文件读取/写入字节 |
| --- | ---: | ---: | ---: | ---: |
| B01 | 100000 | 4.570685 | 1 | 0 / 840321 |
| B01 | 1000000 | 33.624534 | 4 | 0 / 8269553 |
| B02 | 1 | 0.020931 | 4 | 0 / 0 |
| B02 | 10 | 0.109242 | 4 | 0 / 0 |
| B02 | 100 | 0.949314 | 4 | 0 / 0 |
| B03 append | 10000 | 0.399854 | 5 | 0 / 100603 |
| B03 correct | 1000 | 5.324184 | 5 | 2166749 / 2192918 |
| B01 | 10000000 | 339.204398 | 40 | 0 / 82695558 |
| B04 | 10 | 0.063546 | 11 | 4235 / 4511 |
| B04 | 100 | 0.064306 | 101 | 4235 / 4511 |
| B04 | 1000 | 0.063883 | 1001 | 4235 / 4511 |

1000 万行由受限批次流式生成，分为 10 个独立原子范围提交，不是一次超限原子写入。最终当前文件 40 个、当前范围 10 个，没有正常业务行乘多套流程表。RSS 实测峰值 512815104 字节，单次原子范围 scratch 峰值 15214090 字节。100 次无变化输入保持 100 万行、同一 4 文件集合，未生成业务文件；日志从 305 增至 29831 字节，仍受统一上限控制。B03 追加只写一个新文件；纠错仅替换一个受影响文件。B04 的当次读取和写入未随背景分区线性放大。

文件字节是逻辑受影响压缩文件大小，不是全设备 I/O。无变化路径的操作 RSS 采样 0 不表示进程不占内存。所有负载是合成开发数据，不是生产性能 SLA 或全量完成时间承诺。

原始产物 `lf-d01-isolated-acceptance`，artifact ID `10846737169`，GitHub 保留 7 天。下载 ZIP 的 SHA-256 为 `c3adb6665896e62c0ced304195bccccb469619b6dd205c1017b02643e31e9fb1`；其中 `lfd01-result.json` 为 18857 字节，SHA-256 `45321a60fb9e0b07319cadf02e6498b60ad38ecb6d048917dbffb545826cbbf6`。产物已下载并逐项核对；上述必要指标在本文件长期保留，原本地完整日志和原始容量报告继续保留在 `evidence/lf-d01-*`。

## 失败、未执行与剩余边界

最初通过临时 Actions token 推送包含 workflow 的补丁被 GitHub 正常拒绝，因为该 token 没有 workflow 写入权限。之后仅用它推送源码，workflow 通过已授权的 GitHub 连接单独提交；最终源码树一致，未绕过分支保护、测试或权限。

之前阻断 D01 的三项——GitHub 写入、真实跨容器/支持文件系统验收、本次前端及远程完整 CI——均已有实际成功证据。当前没有已知失败的 D01 开发验收项。文档更新后仍等待最终 head CI，再按已授权流程合并；本文件不会提前宣称合并已完成。

未执行且不属于本包：D02 真实来源适配和语义验收、D03 旧体系 reset/退役、D04 公共 API/默认调度、D05 最终集成全套验收。不得用本包结果宣称整个 LF-01 已完成。

## 权限自述与后续输入

生产访问、内网/供应商连接、部署、停写、迁移、调度修改、reset、rebuild、业务写入/删除/清理全部**未执行**。只在临时开发数据库和空测试目录中执行合成读写及故障测试；清理仅限此隔离 Compose 项目自行创建的容器、测试卷和目录，没有全局 prune。

后续以本 PR 实际合并到 main 的提交作为 D02 输入，并参考接口文档，不再应用旧 local-only 补丁。部署仍由维护者负责。`lf_d01_acceptance_complete=true`（开发包）；`lf01_code_ready=false`；`reset_applied=false`；`rebuild_complete=false`；`domains_accepted=false`。
