import { readApiToken } from "../auth/tokenStorage";
export type BacktestRun = { id:string; run_id?:string; run_kind:string; status:string; terminal_status?:string|null; progress:number; current_date?:string|null; current_step?:string|number|null; last_heartbeat_at?:string|null; message?:string; error_message?:string|null; terminal_reason?:string|null; completion_marker?:Record<string,unknown>|null; results?:Record<string,unknown>; child_pid?:number|null; child_start_identity?:string|null; child_process_group_id?:number|null; worker_id?:string|null; worker_handshake_at?:string|null; cancel_requested?:boolean; termination_requested_at?:string|null; termination_reason?:string|null; runner_exit_code?:number|null; runner_exit_category?:string|null; runner_exit_report?:Record<string,unknown>|null; completion_marker_validation?:Record<string,unknown>|null; result_integrity_status?:string|null; result_integrity_evidence?:Record<string,unknown>|null; result_counts?:Record<string,unknown>; stdout_bytes?:number|null; stdout_digest?:string|null; stdout_truncated?:boolean|null; resource_limit_evidence?:Record<string,unknown>|null; recovery_observed_at?:string|null; recovery_action?:string|null; recovery_process_state?:Record<string,unknown>|null };
async function request(path:string, init:RequestInit={}) { const r=await fetch(path,{...init,headers:{"Content-Type":"application/json",Authorization:`Bearer ${readApiToken()||""}`,...init.headers}}); if(!r.ok) throw new Error((await r.json().catch(()=>({}))).detail||"回测请求失败"); return r.json(); }
export const listBacktestRuns=()=>request("/api/admin/backtest-runs");
export const createBacktestRun=(payload:unknown)=>request("/api/admin/backtest-runs",{method:"POST",body:JSON.stringify(payload)});
export const getBacktestRun=(id:string)=>request(`/api/admin/backtest-runs/${id}`);
export const cancelBacktestRun=(id:string)=>request(`/api/admin/backtest-runs/${id}/cancel`,{method:"POST"});
export const compareBacktestRuns=(run_ids:string[])=>request(`/api/admin/backtest-runs/compare`,{method:"POST",body:JSON.stringify({run_ids})});
